-- Violence District: open key access before requesting the main script.
-- Get a key: https://dtrx.duckdns.org/
-- Discord: https://discord.gg/SF9Jm86v8g
local ok, loader = pcall(function()
    return game:HttpGet("https://dtrx.duckdns.org/loader.lua")
end)
if not ok then
    return warn("[VD] Key service unavailable. Please try again later.")
end
local start, err = loadstring(loader)
if not start then
    return warn("[VD] Unable to load key access: " .. tostring(err))
end
start()
